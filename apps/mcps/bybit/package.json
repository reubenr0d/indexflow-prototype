{
  "name": "bybit-mcp",
  "version": "1.0.0",
  "description": "Read-only MCP server for Bybit V5 public perp market data (mark/index price, OI, funding history, kline vol). Required by agents/funding-rate-harvester.md. Defaults to testnet when BYBIT_TESTNET is unset in this process; public endpoints need no API key.",
  "private": true,
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "smoke": "node index.js --smoke",
    "test": "node --test symbol-mapping.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.23.0"
  }
}
