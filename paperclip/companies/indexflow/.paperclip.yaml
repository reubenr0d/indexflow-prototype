# Generated by scripts/sync-paperclip-mirror.mjs — do not edit by hand.
agents:
  self-improver-issues:
    adapter:
      type: process
      config:
        command: npm
        args: ["run","agent:run","--","self-improver-issues"]
    inputs:
      env:
        LLM_API_KEY:
          kind: secret
          requirement: required
        LLM_BASE_URL:
          kind: secret
          requirement: required
        LLM_MODEL:
          kind: secret
          requirement: required
        GH_TOKEN:
          kind: secret
          requirement: required
        AGENT_NETWORK:
          kind: secret
          requirement: required
        AGENT_NON_INTERACTIVE_WRITE_EXECUTE:
          kind: secret
          requirement: required
        AGENT_MAX_TURNS:
          kind: secret
          requirement: required
        LLM_MODEL_SELF_IMPROVER_ISSUES:
          kind: secret
          requirement: optional
  issue-implementer:
    adapter:
      type: process
      config:
        command: npm
        args: ["run","agent:run","--","issue-implementer"]
    inputs:
      env:
        LLM_API_KEY:
          kind: secret
          requirement: required
        LLM_BASE_URL:
          kind: secret
          requirement: required
        LLM_MODEL:
          kind: secret
          requirement: required
        GH_TOKEN:
          kind: secret
          requirement: required
        AGENT_NETWORK:
          kind: secret
          requirement: required
        AGENT_NON_INTERACTIVE_WRITE_EXECUTE:
          kind: secret
          requirement: required
        AGENT_MAX_TURNS:
          kind: secret
          requirement: required
        LLM_MODEL_ISSUE_IMPLEMENTER:
          kind: secret
          requirement: optional
