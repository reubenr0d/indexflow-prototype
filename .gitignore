# Compiler files
cache/
out/
lcov.info

# Ignores development broadcast logs
!/broadcast
/broadcast/*/31337/
/broadcast/**/dry-run/

# Dotenv files (templates stay tracked: `.env.example`, `**/.env.example`)
.env
.env.local
apps/web/.env.local

node_modules/

apps/web/test-results/
apps/web/playwright-report/
.vercel

# Agent memory and published metadata now live on 0G Storage. The local
# folders are warm-restart caches / debug artefacts only — see
# docs/AGENTS_FRAMEWORK.md for details.
agents/memory/
apps/web/public/agent-metadata/

.DS_Store
