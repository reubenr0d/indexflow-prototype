# Agent Deployment Memory

Local allowlist/ownership ledger for deployment operations.
Agents must read this file before touching cloud resources.

Last updated: 2026-05-27 02:25 UTC+05:30 — **CI vault-agent matrix expanded from 2 to 7 agents on Mantle Sepolia round-robin.** [`.github/workflows/vault-agent.yml`](.github/workflows/vault-agent.yml) now picks one trading agent per `:18` tick via `HOUR_UTC % 7` across `mining-manager` / `quality-matrix-manager` / `rwa-treasurer` / `meth-carry-manager` / `rwa-yield-router` / `funding-rate-harvester` / `smart-money-mirror-manager` against the Mantle Sepolia hub at chain `5003`. Three new MCP servers shipped (`apps/mcps/bybit`, `apps/mcps/nansen`, `apps/mcps/rwa-adapter`) with companion skills under [`agents/skills/`](agents/skills/). `BasketVault` gained RWA hooks (`setRWAAdapter`, `allocateToRWA`, `withdrawFromRWA`, `harvestRWAYield`, `rotateReserveToken`, `pendingRedemptionsUsdc`) — ABIs regenerated under [`apps/web/src/abi/`](apps/web/src/abi/) and [`apps/envio/abis/`](apps/envio/abis/). **Required secrets already set on `reubenr0d/indexflow-prototype`:** `ENVIO_URL`, `MANTLE_SEPOLIA_RPC_URL`, `KEEPER_PRIVATE_KEY`, `LLM_API_KEY`, `BYBIT_API_KEY`, `BYBIT_API_SECRET` (verified via `gh secret list`; local `.env` also carries `BYBIT_*` for `funding-rate-harvester` dry-runs). **Operator follow-ups required to unblock the three RWA agents** (`rwa-treasurer`, `meth-carry-manager`, `rwa-yield-router`): (1) run `CHAIN=mantle-sepolia forge script Deploy.s.sol --rpc-url mantle_sepolia --broadcast` to deploy the Mantle RWA testnet stack (`MockUSDY` + `MockUSDYInstantManager`, `MockMUSD` + `MockMUSDWrapper`, `MockMETH` + `MethAdapter`) and refresh [`apps/web/src/config/mantle-sepolia-deployment.json`](apps/web/src/config/mantle-sepolia-deployment.json) with the new `rwa: {...}` + `rwaAssetIds: {...}` blocks; (2) deploy a per-vault `RWAReserveAdapter` for each managed vault and call `BasketVault.setRWAAdapter(adapter)` + `BasketVault.setRWATargetBps(7000)` against it (the adapter pins its `vault` address at construction so it cannot be deployed chain-wide); (3) start the keeper's USDY/METH price-relayer so `submitPrice` is called against `USDY-USDC` / `METH-USDC` asset ids before the agents' first tick (otherwise `adapter.getReserveValueUsdc()` reverts on `RWA price stale`). The four agents that do NOT depend on the RWA stack (`mining-manager`, `quality-matrix-manager`, `funding-rate-harvester`, `smart-money-mirror-manager`) can take their slot immediately. **`funding-rate-harvester` sponsor stack:** `BYBIT_API_KEY` / `BYBIT_API_SECRET` are set in GitHub secrets and local `.env`; v1 still uses read-only V5 market endpoints (keys are passthrough for the v2 Byreal execution stretch). **Optional:** `NANSEN_API_KEY` (unset → `smart-money-mirror-manager` degrades to `nansen_mode: "envio_only"` against Mantle DEX swap events). **Smoke-test pattern after secrets land:** `gh workflow run vault-agent.yml --repo reubenr0d/indexflow-prototype -f agent=<slug> -f dry_run=true -f network=mantle-sepolia` per agent, then re-run with `dry_run=false` once the dry-run write batch matches expectations.

Previous (Envio HyperIndex slug `31d1482` recovered after a privileged `ENVIO_API_TOKEN` was set on the Envio Cloud indexer — the previously-stuck Sepolia chain `11155111` now reports `num_events_processed = 2,594` at block `10,928,309` and 4 hub baskets are indexed; Mantle Sepolia `5003` also unblocked at 4 events. Root cause was the shared fallback API token baked into `envio` package (`node_modules/envio/src/sources/HyperSyncSource.res.js`) being rate-limited (HTTP 429) by Sepolia HyperSync, and `ENVIO_HYPERSYNC_CLIENT_MAX_RETRIES=0` swallowing the throttle as silent empty batches. **Vercel `NEXT_PUBLIC_ENVIO_URL` rotated to `https://indexer.dev.hyperindex.xyz/31d1482/v1/graphql` on all 3 environments (Production / Preview / Development); production redeploy of `snx-prototype-nti91nmjl-…` triggered via `vercel redeploy` — new build `snx-prototype-ku95w1v84-…` in flight.** `apps/web/.env.local` and the GitHub `ENVIO_URL` secret are still on the old slug and need a manual operator rotation. Paperclip local control-plane install planned at `~/.paperclip/instances/default/` per the `npx paperclipai onboard` default; owner `user`, agent `read` only — see [`COMPANY.md`](COMPANY.md) for the repo-side IndexFlow company manifest the `paperclip-agent-companies-plugin` will import, and [`docs/PAPERCLIP_RUNBOOK.md`](docs/PAPERCLIP_RUNBOOK.md) for the install + first-heartbeat runbook. Theseus sovereign-agent pilot deploy is scaffolded but `planned` — see the Theseus row below + [`agents/mining-manager-theseus.md`](agents/mining-manager-theseus.md))

## Policy

- Resources not listed here are treated as protected (`read` only).
- Existing deployments are user-owned unless explicitly marked otherwise.
- Agent-created resources must be recorded immediately after creation.

## Resources

| Provider | Project/Account | Resource Type | Resource Name | Environment | Owner | Allowed Actions | Purpose | Created |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| GCP | `industrial-joy-440019-h3` | Cloud Run Service | `PUSH_WORKER_SERVICE` (from env/secrets) | production | user | `read`, `deploy`, `update-config` | Web Push API/dispatch worker for PWA notifications | 2026-04-10 |
| GCP | `industrial-joy-440019-h3` | Cloud Scheduler Job | `PUSH_REALTIME_JOB_NAME` (from env/secrets) | production | user | `read`, `deploy`, `update-config` | Triggers realtime push dispatch cadence | 2026-04-10 |
| GCP | `industrial-joy-440019-h3` | Cloud Scheduler Job | `PUSH_DIGEST_JOB_NAME` (from env/secrets) | production | user | `read`, `deploy`, `update-config` | Triggers digest push dispatch cadence | 2026-04-10 |
| Vercel | org/project from CI secrets | Web Deployment | `apps/web` production deployment | production | user | `read`, `deploy` | Production Next.js web app deployment | 2026-04-10 |
| The Graph Studio | Studio account `867` | Subgraph (deprecated) | `indexflow-prototype` (Sepolia, label `0.2.1`) | production | user | `read` only | Deprecated subgraph replaced by Envio HyperIndex (2026-05-20 cutover). User to archive in The Graph Studio when convenient; agent must not redeploy or modify. | 2026-04-17 |
| Envio | Envio Cloud org `reubenr0d` | HyperIndex deployment | `indexflow-prototype-3` (auto-deploy from `reubenr0d/indexflow-prototype@main`, root `apps/envio`, dev tier) | production | agent | `read`, `deploy`, `update-config` | Single Hasura GraphQL endpoint serving every chain (Sepolia hub `11155111`, Fuji spoke `43113`, Arbitrum Sepolia `421614`, Mantle Sepolia hub `5003` per [`apps/envio/config.yaml`](apps/envio/config.yaml)). Auto-deploys on push to `main`. **Current URL** `https://indexer.dev.hyperindex.xyz/31d1482/v1/graphql` (deployment slug `31d1482` = commit `a9bfc32`). **RESOLVED 2026-05-27 ~01:20 UTC+05:30:** Sepolia (`11155111`) is now processing — `num_events_processed = 2,594`, `latest_processed_block = 10,928,309`, `first_event_block_number = 10,891,792`, and 4 hub baskets indexed (`Minestarters ML Picks` `0x4dcd…a0ba`, `Minestarters Quality Matrix` `0xbd7e…41f3`, `Multichain Test Basket 18-43` `0x2c4d…2069`, `Minestarters Precious Metals` `0x4946…d0fe`). Mantle Sepolia (`5003`) also unblocked with 4 events at block `39,140,266`. Fuji (`43113`) continues at 67 events. **Root cause** of the prior silent-zero outage: the `envio` runtime falls back to a hardcoded shared API token (`3dc856dd-…6e07`, in `node_modules/envio/src/sources/HyperSyncSource.res.js`) when `ENVIO_API_TOKEN` is unset, and Sepolia HyperSync was rate-limiting that shared token (HTTP 429). Combined with `ENVIO_HYPERSYNC_CLIENT_MAX_RETRIES = 0` (the Rust binary default), throttled requests surfaced as silent empty batches and `progressBlock = -1` instead of an error. **Fix applied:** the user's personal token `ENVIO_API_TOKEN` was set on the Envio Cloud indexer via `envio-cloud indexer env set ENVIO_API_TOKEN <token>`; Envio Cloud hot-applied it to the running deployment without a slug rotation. The working-tree `rpc: for: sync` override on `11155111` (added during diagnosis to mirror the `5003` Mantle Sepolia pattern) is **no longer required** and should be reverted before the next push so Sepolia stays on the faster HyperSync path. Set `SEPOLIA_RPC_URL`, `FUJI_RPC_URL`, `ARBITRUM_SEPOLIA_RPC_URL`, and `MANTLE_SEPOLIA_RPC_URL` in the Envio Cloud env panel so handler-time `eth_call` enrichment works on every chain. Schema root entities: `Basket` (one row per BasketCreated, fields `name`, `creator`, `chainId`, `createdAt`, `assetCount`, `vault`, `shareToken`, `basketPrice`, `tvlBookUsdc`, `usdcBalanceUsdc`); `BasketActivity` (per-event log with `activityType`, `amountUsdc`, `txHash`, `timestamp`, `user`, `basket`); `BasketAsset` / `BasketExposure` / `BasketSnapshot` / `UserBasketPosition` for sub-entities. **Vercel rotation (done 2026-05-27):** `NEXT_PUBLIC_ENVIO_URL` updated on all 3 environments (Production / Preview / Development) of the `snx-prototype-web` project to `https://indexer.dev.hyperindex.xyz/31d1482/v1/graphql`; production redeploy of `snx-prototype-nti91nmjl-…` triggered (`vercel redeploy --target production`), new build `snx-prototype-ku95w1v84-…` in flight. **Still on the old slug (operator action required):** `apps/web/.env.local` line 2 (`NEXT_PUBLIC_ENVIO_URL=https://indexer.dev.hyperindex.xyz/822ce13/v1/graphql`) and the GitHub Actions `ENVIO_URL` secret on `reubenr0d/indexflow-prototype` — rotate with `gh secret set ENVIO_URL --body "https://indexer.dev.hyperindex.xyz/31d1482/v1/graphql" --repo reubenr0d/indexflow-prototype`. **Previous URLs (history):** `115a80f` (rotated 2026-05-26 23:30 UTC+05:30; build failed closed on Mantle Sepolia HyperSync gap), `822ce13` (rotated 2026-05-26; pre-Mantle multichain), `dbe3f66` (rotated 2026-05-22), `caee388` (pre-rename `-2` indexer). Indexer was renamed from `indexflow-prototype-2` → `indexflow-prototype-3` on 2026-05-21. Manage via `envio-cloud …` (agent has session in `~/.envio-cloud`). | 2026-05-20 (URL rotated 2026-05-27) |
| GCP | `watchful-gear-493003-t8` | Cloud Run Service | `indexflow-push-worker` | production | agent | `read`, `deploy`, `update-config` | Serverless Web Push API/dispatch worker for PWA notifications | 2026-04-11 |
| GCP | `watchful-gear-493003-t8` | Cloud Scheduler Job | `indexflow-push-realtime` | production | agent | `read`, `deploy`, `update-config` | Realtime dispatch trigger (`*/5 * * * *`) | 2026-04-11 |
| GCP | `watchful-gear-493003-t8` | Cloud Scheduler Job | `indexflow-push-digest` | production | agent | `read`, `deploy`, `update-config` | Digest dispatch trigger (`0 */6 * * *`) | 2026-04-11 |

| Ethereum Sepolia | Chain `11155111` | Smart Contract | `PoolReserveRegistry` (`0xEA3F5EC162F0b583B10E2e14506aC1e0aD4Cf449`) | sepolia | agent | `read` | TWAP-based GMX pool depth tracking and proportional routing weights | 2026-04-17 |
| Ethereum Sepolia | Chain `11155111` | Smart Contract | `CCIPReserveMessenger` (`0x26ffdD8c87c4057C13e8Ce3491953Bd1e4B6Fe6B`) | sepolia | agent | `read` | Delta-triggered CCIP pool state broadcast/receive | 2026-04-17 |
| Ethereum Sepolia | Chain `11155111` | Smart Contract | `IntentRouter` proxy (`0xa71220762Ff6C0e8D55d492da3265D56C279d2cE`) | sepolia | agent | `read`, `deploy` | UUPS-upgradeable intent escrow and execution router (hub-only; spokes do not deploy IntentRouter) | 2026-04-17 |
| Ethereum Sepolia | Chain `11155111` | Smart Contract | `IntentRouter` impl (`0x730400eEA631f8C4b31066483f6042732C1C783D`) | sepolia | agent | `read` | IntentRouter logic implementation (hub-only) | 2026-04-17 |
| Ethereum Sepolia | Chain `11155111` | Smart Contract | `CrossChainIntentBridge` (`0x1aC98b06a556815C1e0FfE46fdD69Ed489435b8F`) | sepolia | agent | `read` | Stateless CCIP relay for cross-chain deposit intents (hub-only; spokes do not deploy IntentBridge) | 2026-04-17 |
| Ethereum Sepolia | Chain `11155111` | Smart Contract | `OracleConfigQuorum` (`0x446A26005bc82FcC7eB725413229ecBF65366548`) | sepolia | agent | `read` | Quorum-based oracle config consensus via CCIP (address will change on redeploy) | 2026-04-17 |
| Ethereum Sepolia | Chain `11155111` | Smart Contract stack (clean redeploy) | Sepolia full stack from `apps/web/src/config/sepolia-deployment.json` (block `10891752`): `BasketFactory 0xa9a83c9383da6ea831504f79c68e3230b604145e`, `VaultAccounting 0xc4a01a3eada32f8ebe1cc05407cd7ef9c2dc86d8`, `OracleAdapter 0x7edb8cf201d1bfd9e028c94c955fcc161978e7ea`, `PerpReader 0x8d0F44C14324e9b6FBA1a0f7C8F4EF8f0Ee555E1`, `PricingEngine 0xcE07DE0b8B3F188aFCF5796f612C25bCe792cB4C`, `FundingRateManager 0xB10001F737d036f5590D8ad486d2dB302A0Afb4A`, `PriceSync 0x886d313E167A7a3A3fcA5d42f720E24159BbDD12`, `MockUSDC 0x7Cab35e58088d161C0e832279C56Fbd1b0679D67`, `GMXVault 0x3393C69fc86C7128d9450258e0A2eF6c2DB1D696`, `AssetWiring 0x416dE70cCf39b2194CC101E099371A4E21bfD3ce`, `StateRelay 0xA2DcF0B25fcE90B5F5D89ADD2Af1eCC101bBcc7d` | sepolia | agent | `read`, `deploy`, `update-config` | 2026-05-21 clean redeploy via `CHAIN=sepolia forge script Deploy.s.sol --slow` (41 receipts, broadcast `broadcast/Deploy.s.sol/11155111/run-latest.json`, deployer wallet `0x36716C8C5D1ae680C78bD0ECc230896556399713`). Includes the `BasketFactory ← VaultAccounting.setWirer(true)` fix baked into `Deploy.s.sol` at L199, so the 2026-05-21 17:06 manual `setWirer` follow-up is no longer needed. BHP.AX wired as the bootstrap oracle asset by the in-script `aw.wireAsset(...)`. One keeper epoch posted post-deploy (`npm run keeper:once`). Supersedes the pre-2026-05-21 Sepolia BasketFactory/VaultAccounting/OracleAdapter/StateRelay addresses (old `BasketFactory 0x9c80…fec4`, `VaultAccounting 0xb70c…b269`, `OracleAdapter 0x6932…d399`, `StateRelay 0xb837…9CD9`, `priceSync 0xa6a5…d06`, `MockUSDC 0xf0e4…8585`, `GMXVault 0x6797…4f9`, `AssetWiring 0xf6a6…17b`). | 2026-05-21 |
| Avalanche Fuji | Chain `43113` | Smart Contract stack (clean redeploy) | Fuji spoke stack from `apps/web/src/config/fuji-deployment.json` (block `55607589`): `BasketFactory 0xb797210b3A6315726bC829599B8b2435FEa53C29`, `StateRelay 0xF6fa7d879eF8aEed74B62121c7F8558eECe3a515`, `RedemptionReceiver 0x38041d12DAA38ca05F5b9F87f17BfE3c3123b5C4`, `bootstrapBasket 0x40a97cB94cb08a91D3C4539651E4F6EfA14eeFd1` ("Fuji Bootstrap Basket"), `MockUSDC 0x09e34E0FCA591BbFe7759eD9927e17cd3cd135D8` | fuji | agent | `read`, `deploy`, `update-config` | 2026-05-21 clean redeploy via `CHAIN=fuji forge script DeploySpoke.s.sol --slow` (12 receipts, broadcast `broadcast/DeploySpoke.s.sol/43113/run-latest.json`). StateRelay keeper set to deployer wallet `0x36716C8C5D1ae680C78bD0ECc230896556399713` (KeeperHub removed per 2026-05-20 CHANGELOG; the previous Fuji `setKeeper(0xBAefbc6D…e409)` row is no longer applicable). Bootstrap basket auto-wired to the new StateRelay in DeploySpoke step 4 (`basket.setStateRelay(ctx.stateRelay)`). RedemptionReceiver `setTrustedSender(...)` is **not** wired yet — there is no Sepolia-side redemption sender contract in the current stack; deferred until that hub-side counterpart exists. Supersedes the pre-2026-05-21 Fuji `BasketFactory 0x7431…d42e`, `StateRelay 0x0A56…22A2`, `RedemptionReceiver 0x2C3B…fb93`, `bootstrapBasket 0xD097…A6E4`, `MockUSDC 0x8E80…53fB`. **Fix landed in this redeploy**: `script/DeploySpoke.s.sol` patched to drop the inner `vm.startPrank(ctx.deployer)` block in `_maybeBootstrapSpokeBasket` (Forge ≥1.5 rejects `prank` while a broadcast is active with `cannot prank for a broadcasted transaction`); the active outer `vm.startBroadcast(deployerPrivateKey)` already pins `msg.sender` to the deployer for the `approve` / `topUpReserve` calls. | 2026-05-21 |
| ~~Ethereum Sepolia~~ | Chain `11155111` | ~~Smart Contract~~ | ~~`StateRelay` (pending deploy)~~ | sepolia | agent | `read` | **SUPERSEDED 2026-05-21** by the consolidated Sepolia clean-redeploy row above; new StateRelay is `0xA2DcF0B25fcE90B5F5D89ADD2Af1eCC101bBcc7d`. | 2026-04-17 |
| ~~Ethereum Sepolia~~ | Chain `11155111` | ~~Smart Contract~~ | ~~`RedemptionReceiver` (pending deploy)~~ | sepolia | agent | `read` | **SUPERSEDED 2026-05-21**: no Sepolia-side RedemptionReceiver was deployed in this redeploy. The hub-side redemption sender contract is still TBD; the Fuji-side `RedemptionReceiver 0x38041d…b5C4` is recorded in the Fuji clean-redeploy row above. | 2026-04-17 |
| ~~Avalanche Fuji~~ | Chain `43113` | ~~Smart Contract~~ | ~~`StateRelay` (pending deploy)~~ | fuji | agent | `read` | **SUPERSEDED 2026-05-21** by the Fuji clean-redeploy row above; new StateRelay is `0xF6fa7d879eF8aEed74B62121c7F8558eECe3a515`. | 2026-04-17 |
| ~~Avalanche Fuji~~ | Chain `43113` | ~~Smart Contract~~ | ~~`RedemptionReceiver` (pending deploy)~~ | fuji | agent | `read` | **SUPERSEDED 2026-05-21** by the Fuji clean-redeploy row above; new RedemptionReceiver is `0x38041d12DAA38ca05F5b9F87f17BfE3c3123b5C4`. | 2026-04-17 |
| ~~Avalanche Fuji~~ | Chain `43113` | ~~Smart Contract config~~ | ~~`StateRelay.keeper()` on `0x0A564071Fb11A0b84314f0B78AC69C5742Db22A2` (set to KeeperHub wallet `0xBAefbc6D…e409`)~~ | fuji | agent | `read` | **SUPERSEDED 2026-05-21**: the old StateRelay is no longer referenced. The new Fuji StateRelay `0xF6fa7d…a515` has `keeper() == deployer wallet 0x36716C8C…9713` (KeeperHub removed per CHANGELOG 2026-05-20). | 2026-04-27 |
| ~~Avalanche Fuji~~ | Chain `43113` | ~~Off-chain wallet~~ | ~~KeeperHub-managed wallet `0xBAefbc6D66C6ACc123374A0ABa10E946b932e409`~~ | fuji | agent | `read` | **SUPERSEDED 2026-05-21**: KeeperHub integration removed per CHANGELOG 2026-05-20; this wallet is no longer in the keeper path. Any residual AVAX balance on it is now dust. | 2026-04-27 |
| Minestarters | atlas-prototype (user-managed VM) | External HTTPS API | `https://atlas.minestarters.com/api/v1/...` (ml-rankings, ml-model-info, ml/basket, ml/thesis) | production | user | `read` only | Atlas mining-stock ML engine consumed by `mining-manager` agent via the `atlas-ml-mcp` server. Agent never modifies the Atlas deployment; only issues GET requests. | 2026-05-21 |
| Minestarters | atlas-prototype (user-managed VM) | External HTTPS API | `https://atlas.minestarters.com/api/v1/...` (dashboard/stocks/baskets, dashboard/company/{ticker}/profile, dashboard/stocks/events, dashboard/news/feed) | production | user | `read` only | Atlas read-only data endpoints consumed by `quality-matrix-manager` agent via the `atlas-quality-mcp` server. Agent never modifies Atlas; only issues GET requests. Tier scoring is computed locally in `apps/mcps/atlas-quality/scoring/matrix.js` from `matrix.json`. | 2026-05-21 |
| Ethereum Sepolia | Chain `11155111` | Basket Vault (deployed by agent on first run) | `Minestarters Quality Matrix` vault (address recorded in `agents/memory/quality-matrix-manager/state.json` after first deployment) | sepolia | agent | `read`, `deploy`, `update-config` | Long/short mining vault driven by the analyst's 8-category Quality Matrix scorer. Deployed and managed exclusively by the `quality-matrix-manager` agent via `vault-manager-mcp.create_vault`. Agent is bound to this single vault; it must not touch the parallel `Minestarters ML Picks` vault owned by `mining-manager`. On 2026-05-22 the agent ran `setStateRelay(0xA2DcF0…cc7d)` against the Sepolia vault (tx `0x084092a1…fbdc6f5`) to wire the live `StateRelay` so the deposit weight gate becomes active; this is `update-config` only, no holdings/positions touched. | 2026-05-21 |
| Avalanche Fuji | Chain `43113` | Basket Vault (twin of Sepolia ML Picks) | `Minestarters ML Picks` Fuji twin (`0x6180f8d6cdd3f5d42d00130b52ab635a2d618f53`) | fuji | agent | `read`, `deploy`, `update-config` | Spoke-side twin of the Sepolia `Minestarters ML Picks` vault, created on 2026-05-22 via `BasketFactory(0xb79721…3c29).createBasket("Minestarters ML Picks", 50, 50)` (tx `0xde70614e…a90aad12`) so the multi-chain deposit drawer in `apps/web` can route the Fuji-side share of a multi-chain split into a name-matched local vault (`useVaultAddressByName` lookup). Wired with `setStateRelay(0xF6fa7d…a515)`, `setAssets([keccak256("USDC")])` (stub asset id — Fuji has no oracle adapter; pattern matches `script/DeploySpoke.s.sol::_maybeBootstrapSpokeBasket`), and pre-seeded with 1,000 mUSDC via `topUpReserve`. Agent (`mining-manager`) continues to operate against the Sepolia original only; this twin exists purely to receive multi-chain deposit splits. Agent must NOT call `open_position` / `allocate_to_perp` against this Fuji address — spokes have no perp stack. | 2026-05-22 |
| Avalanche Fuji | Chain `43113` | Basket Vault (twin of Sepolia Quality Matrix) | `Minestarters Quality Matrix` Fuji twin (`0x435847fa2f0c91c6d39067ab85c0cd9234d2b979`) | fuji | agent | `read`, `deploy`, `update-config` | Spoke-side twin of the Sepolia `Minestarters Quality Matrix` vault, created on 2026-05-22 via `BasketFactory(0xb79721…3c29).createBasket("Minestarters Quality Matrix", 50, 50)` (tx `0x2939c8f4…c9051471`). Wired with `setStateRelay(0xF6fa7d…a515)`, `setAssets([keccak256("USDC")])`, pre-seeded with 1,000 mUSDC. Same usage rules as the ML Picks twin above — purely a deposit-routing target; agent (`quality-matrix-manager`) continues to operate against the Sepolia original only. | 2026-05-22 |
| GCP (planned) | TBD project (to be created in Week 0) | Cloud Run Service | `indexflow-leaderboard-worker` | production | user | `read` | **PLANNED — not yet deployed.** Season 1 Operator Trials leaderboard fusion worker (per [growth/X_GROWTH_PLAN.md](growth/X_GROWTH_PLAN.md) + [growth/GALXE_CAMPAIGN_PLAN.md](growth/GALXE_CAMPAIGN_PLAN.md)). Reads Galxe `GetLeaderboard` GraphQL + Boost.xyz claim state + Envio quality-multiplier inputs, computes multi-dimensional ranking (raw points × quality multiplier × streak × decay × guild weight), publishes a static JSON snapshot the `/operators` Hall of Fame page consumes. Owner: **user** (Reuben deploys + manages — public-facing brand surface). Agent role limited to `read` once live; agent must not deploy, restart, or reconfigure. AGENT_DEPLOYMENT_MEMORY row will be re-keyed from `planned` → `live` with the concrete project/service name on first deploy. | 2026-05-23 (planned) |
| Vercel (planned) | Reuben's existing `apps/web` project | Web API Route | `apps/web/src/app/api/galxe/credential/route.ts` (Galxe dynamic credential REST endpoint) | production | user | `read` | **PLANNED — not yet deployed.** Galxe Educators Guild dynamic-check endpoint (per [growth/GALXE_CAMPAIGN_PLAN.md](growth/GALXE_CAMPAIGN_PLAN.md)). Verifies on-demand conditions Galxe cannot self-verify (quiz pass attestation, GitHub PR merge state, doc visit verified via Envio session). Co-deploys with the existing `apps/web` Vercel production deployment — no new GCP project, no separate service. Owner: **user** (lives inside the user-owned `apps/web` deployment). Agent role limited to `read` of the route source; agent must not modify the live endpoint behaviour. | 2026-05-23 (planned) |
| X (planned) | New brand account (separate from `@indexflowDAO`) | External X account + bot | `@IndexFlowBots` (or similar — handle TBD) | production | user | `read` | **PLANNED — not yet deployed.** Season 1 auto-broadcast bot (per [growth/X_GROWTH_PLAN.md](growth/X_GROWTH_PLAN.md) "X-native mechanics"). Watches `BasketCreated` events via Envio HyperIndex GraphQL subscription on the hub testnet, posts a templated tweet introducing each new basket (name, curator handle if resolvable from a verified bio, asset count, hub link). Owner: **user** (Reuben holds the X credentials — brand-sensitive public-facing). Agent role limited to `read` of the bot's run logs; agent must not authenticate as the bot or post on its behalf. Bot service may run on the same `indexflow-leaderboard-worker` Cloud Run instance (above) as a separate handler, or on a dedicated Cloud Run service — TBD at deploy time. | 2026-05-23 (planned) |
| ~~Ethereum Sepolia~~ | Chain `11155111` | ~~Smart Contract~~ | ~~`VaultAccounting` (`0xb70ce09018bD05D79AdFB1B78d9F42156804b269`)~~ | sepolia | agent | `read` | **SUPERSEDED 2026-05-21** by the consolidated Sepolia clean-redeploy row above; the entire perp stack is fresh. The new VaultAccounting is `0xc4a01a3eada32f8ebe1cc05407cd7ef9c2dc86d8`. The `getVaultPnL` unit-scaling fix and the `setWirer(BasketFactory, true)` grant both ship in the new deploy automatically (Deploy.s.sol L199). The 2026-05-21 17:06 manual `setWirer` follow-up is obsolete. The old VAs `0xC7aA…CE43` and `0xb70c…b269` are left on-chain but unreferenced. | 2026-05-21 |
| Mantle Sepolia | Chain `5003` | Smart Contract stack (hub) | Mantle Sepolia hub stack from `apps/web/src/config/mantle-sepolia-deployment.json` (block `39077789`): `BasketFactory 0xa07a182040a9EB2a398b7853c2af3Bd2dE5f318C`, `VaultAccounting 0x7c00248d9b1435b82ACf2F44D1E9634D0876ea49`, `OracleAdapter 0x02879FaE6F2d884768f269Ac4D350544352e9264`, `PerpReader 0x280f20caED817C678893f8838Cb867f542337F1A`, `PricingEngine 0x2C3BdC8977B52c4Abfe26b32f6DF3B025A08fb93`, `FundingRateManager 0x743194BAAe87A8A518739a1eB71f04773b46d42e`, `PriceSync 0xB3c318f338d3b9734bD495407ac33Fa79a9a6eA3`, `MockUSDC 0xF982aA442612CCC52270828446A21E85bB12bd3F`, `GMXVault 0xe5Cb69dB42cA2a8F9A98C1d8d47780e402Bd2D2d`, `AssetWiring 0x2272f91c0dA0CE718F3253A6C1738c771E1AAe68`, `StateRelay 0xf4aFB6769b71C06f70a6d717CCf69e5deD962AB5` | mantle-sepolia | agent | `read`, `deploy`, `update-config` | 2026-05-26 Mantle hackathon hub deploy via `CHAIN=mantle-sepolia forge script Deploy.s.sol --slow` (deployer `0x36716C8C5D1ae680C78bD0ECc230896556399713`). `WireStateRelay` ran with 0 vaults (fresh factory). Keeper epoch posted (`KEEPER_CHAINS=mantle-sepolia npm run keeper:once`). Envio network `5003` in `apps/envio/config.yaml`; HyperIndex redeploy slug `115a80f`. Default web deployment target is `mantle-sepolia`. | 2026-05-26 |
| BNB Smart Chain Testnet (planned) | Chain `97` | Smart Contract stack | BNB Smart Chain Testnet spoke stack (planned: `BasketFactory`, `BasketVault` bootstrap basket, `StateRelay`, `RedemptionReceiver`, `MockUSDC`) | bnb-testnet | user | `read` | **PLANNED — not yet deployed.** BNB Smart Chain Testnet EVM spoke for the hackathon submission (tracked in [growth/partnerships/chains/bnb.md](growth/partnerships/chains/bnb.md)). **Prerequisite: verify CCIP testnet lane Sepolia ↔ BNB Smart Chain Testnet is live** (chain selector, router, LINK token must be pinned before deploy). If the lane is unavailable, a degraded "deposit-only with locally-posted state" variant is feasible for the hackathon demo but the multi-chain redemption path is impaired. Deploy path matches Fuji: add `bnb-testnet` to [config/chains.json](config/chains.json), run `CHAIN=bnb-testnet forge script DeploySpoke.s.sol --slow`, write addresses to `apps/web/src/config/bnb-testnet-deployment.json`. Owner: **user** until first deploy. | 2026-05-23 (planned) |
| Alephium testnet (planned) | Alephium testnet (non-EVM) | Smart Contract stack | Alephium testnet spoke stack (planned: shape TBD — Ralph `BasketVault`/`StateRelay`/`RedemptionReceiver`/`MockUSDC` equivalents, or a wrapped/bridged integration) | alephium-testnet | user | `read` | **PLANNED — not yet deployed.** Alephium testnet spoke for the hackathon submission (tracked in [growth/partnerships/chains/alephium.md](growth/partnerships/chains/alephium.md)). **Non-EVM scope blocker:** `script/DeploySpoke.s.sol` is Solidity/Foundry and cannot deploy to Alephium. A real spoke requires either (a) Ralph contracts for `BasketVault`/`StateRelay`/`RedemptionReceiver`/`MockUSDC` plus a new keeper handler in `services/keeper/`, or (b) a wrapped/bridged integration routing Alephium deposits through an existing EVM spoke. CCIP does not support Alephium today; cross-chain messaging needs a different bridge or relayer-based pattern. Owner: **user** until the scope decision is made and a first deploy lands. | 2026-05-23 (planned) |
<!--
=== POST-INSTALL RE-KEY TEMPLATE (do not delete until applied) ===

When Phase 5 of docs/PAPERCLIP_RUNBOOK.md succeeds (manual `self-improver-issues`
heartbeat from the UI produces an entry in heartbeat_runs and writes
agents/memory/self-improver-issues/paperclip-heartbeat.json), replace the
row below with the live-state version pre-staged in
docs/PAPERCLIP_RUNBOOK.md §After Successful Install. The diff is a
single-row replacement — copy-paste, fill the placeholders for actual
port + install date, done.
=== END TEMPLATE ===
-->

| Theseus (planned) | [theseuschain.com](https://theseuschain.com/) sovereign-agent runtime | Sovereign trading agent | `mining-manager-theseus` pilot (sovereign-class twin of repo-managed `mining-manager`) | testnet (then production after burn-in) | user | `read` | **PLANNED — not yet deployed.** Pilot of the Theseus runtime per [`growth/partnerships/theseus.md`](growth/partnerships/theseus.md) MoU (`co_marketing: pending_deploy`). Same trading mandate as the live Sepolia `mining-manager` ([`agents/mining-manager.md`](agents/mining-manager.md)); receipts move from CI-pushed `paperclip-heartbeat.json` to signed onchain attestations per the `proof-of-lobster` skill ([`agents/skills/proof-of-lobster.md`](agents/skills/proof-of-lobster.md), upstream [Theseus](https://github.com/Theseuschain/proof-of-lobster)). Mandate-check uses the existing [`agents/risk-officer.md`](agents/risk-officer.md) prompt + `approve`/`downsize`/`veto` schema, ported inline. Agent holds its own ICA key/balance/state on Theseus — operator (founder) cannot pause the agent once running. Sepolia hub stays canonical for now; Theseus deploy is an **additional surface**, not a replacement. Re-key this row from `planned` → `live` with the concrete agent ID + chain id on first successful testnet receipt; flip [`growth/partnerships/theseus.md`](growth/partnerships/theseus.md) `co_marketing: pending_deploy` → `active` on the same PR. **Out of scope to deploy in this session** — requires founder in-session approval per [`AGENTS.md`](AGENTS.md) §Deployment Safety Rules. | 2026-05-26 (planned) |
| Paperclip (planned) | `~/.paperclip/instances/default/` (default `npx paperclipai onboard` location; entirely separate from this repo's working tree) | Self-hosted control plane (Node.js + embedded Postgres + React UI at `http://localhost:3100`) | Local Paperclip install via `npx paperclipai onboard --yes` plus `npx paperclipai plugin install paperclip-agent-companies-plugin` (note: plugin install uses Paperclip's own CLI via `npx` — NOT `pnpm add` and NOT bare `paperclipai` which isn't on PATH; the plugin hot-loads, no server restart required). The plugin source root MUST point at the generated mirror subdirectory `paperclip/companies/indexflow/` (NOT the repo root — the v0.9.x classifier needs `agents/<slug>/AGENTS.md` per-slug subfolders, which the canonical flat `agents/<slug>.md` files don't provide; the mirror is regenerated via `npm run sync:paperclip`). See [`docs/PAPERCLIP_RUNBOOK.md`](docs/PAPERCLIP_RUNBOOK.md) for the full install + first-heartbeat runbook and [`paperclip/README.md`](paperclip/README.md) for the mirror rationale. | local | user | `read`, `update-config` | **PLANNED — not yet installed.** Operator dashboard for the **IndexFlow** engineering meta-agents and growth/ops agents (NOT the trading agents — `vault-manager`, `mining-manager`, `quality-matrix-manager` and the trading `risk-officer` remain repo-managed via `scripts/agent-runner.mjs` + `.github/workflows/vault-agent.yml` per the scope decision in [`COMPANY.md`](COMPANY.md) §Out of Scope). Imports `COMPANY.md` (schema `agentcompanies/v1`, `name: IndexFlow`, `scope: meta_and_growth_agents`) from this repo and runs each ACTIVE `agents/*.md` employee on a heartbeat via a shell adapter that shells out to `npm run agent:run -- <agent>`. Active employees today: `issue-implementer` (callback-only), `self-improver-issues` (routine paused — CI cron stays canonical), and two `kind: prompt-only` reviewers (`risk-officer-self-improvement`, `risk-officer-self-improvement-issues`) that don't heartbeat. Daily auto-sync (overwrite mode) keeps the Paperclip company state aligned with this repo — **repo is canonical**. Paperclip's Postgres holds runtime history (`heartbeat_runs`, `cost_events`, `activity_log`, tickets, approvals); a lightweight `agents/memory/<agent>/paperclip-heartbeat.json` bridge file is written by `scripts/agent-runner.mjs` on each run and committed back by the `commit-results` job in `.github/workflows/vault-agent.yml`. Brainstormed (not yet active) employees: `content-publisher`, `partnership-tracker`, `broadcast-bot`, `docs-syncer`, `basket-ideator` — each requires a new `agents/<id>.md` prompt file + (in some cases) new skills/MCPs before Paperclip can schedule them. `basket-ideator` is suggest-only and never deploys vault contracts (the repo-managed trading-agent flow owns deployment), preserving the scope boundary. The brainstormed `broadcast-bot` is the same Paperclip-side identity as the planned `@IndexFlowBots` X bot row in this file. Owner: **user** (Reuben installs + manages). Agent role limited to `read` (the agent never invokes the Paperclip CLI or DB directly; the dashboard is for human operators). Re-key from `planned` → `live` on first successful Phase 5 heartbeat per the HTML comment template above + [`docs/PAPERCLIP_RUNBOOK.md`](docs/PAPERCLIP_RUNBOOK.md) §After Successful Install. | 2026-05-26 (planned) |

## Update Template

Use this template for new entries:

| Provider | Project/Account | Resource Type | Resource Name | Environment | Owner | Allowed Actions | Purpose | Created |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `<provider>` | `<project>` | `<type>` | `<name>` | `<env>` | `agent|user` | `<comma-separated-actions>` | `<short-purpose>` | `<YYYY-MM-DD>` |
